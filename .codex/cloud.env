# Codex Cloud environment defaults (CPU-only)
# Source this in tasks or setup scripts: `source .codex/cloud.env`

# Disable CUDA/GPUs in cloud runs
export CUDA_VISIBLE_DEVICES=""

# Common library toggles (opt-in if your stack respects them)
export ACCELERATE_USE_CPU="1"        # 🤗 Accelerate
export JAX_PLATFORM_NAME="cpu"       # JAX
export HF_HUB_DISABLE_TELEMETRY="1"  # Quieter logs

